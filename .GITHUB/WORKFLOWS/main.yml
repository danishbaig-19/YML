name: Build Project
on:
   push:
      branches: 
          -main
jobs:
    build:
       runs-on: ubuntu-latest
       steps:
          - -name: checkout code
            uses: actions/checkout@v3
            -name: Run a rest command
            run: echo "Build Successful"
  
